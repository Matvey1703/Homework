#pragma once

// Fill pi-array for string.
void prefixFunction(char* string, int* pi);

// Position of the first occurrence of the substruct in struct (return -1 if substruct did not found)).
int finder(char* subString, char* string);
